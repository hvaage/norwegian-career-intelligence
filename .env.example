# NAV pam-stilling-feed (technical test / future ingestion)
# Swagger: https://pam-stilling-feed.ekstern.dev.nav.no/swagger
# Docs: https://navikt.github.io/pam-stilling-feed/

# Base URL for the feed API (defaults in code to dev if unset)
NAV_FEED_BASE_URL=https://pam-stilling-feed.ekstern.dev.nav.no

# Optional: Bearer token when the environment requires authentication
# NAV_FEED_TOKEN=
